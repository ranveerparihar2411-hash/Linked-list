#include<stdio.h>
#include<math.h>

int arr=[12,20,302,50,60,400,120,410];

int i,j;

for i in range(len(arr));

  for j in range(0,len(arr)-i-1);
    if arr[j]>arr[i];
       arr[j],arr[j+1]=arr[j+1],arr[j]

print("bubble sort":, arr)
